<%= form_for(@cookie_type) do |f| %>
  <% if @cookie_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@cookie_type.errors.count, "error") %> prohibited this cookie_type from being saved:</h2>

      <ul>
      <% @cookie_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :price %><br>
    <%= f.text_field :price %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
